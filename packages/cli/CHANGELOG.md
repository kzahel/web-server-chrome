# Changelog

## [0.1.1]

### Added
- Static file serving with directory listing
- CORS, SPA mode, and file upload support
- Range request support for streaming
- HTTPS support with self-signed certificate generation
- HTTP/1.1 keep-alive
- Streaming upload support with `--upload` flag
- Backpressure-aware file streaming
- Request timeout and lifecycle error handling
